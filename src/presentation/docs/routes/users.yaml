root:
  get:
    $ref: users/get-users.yaml
me:
  get:
    $ref: users/get-current-user.yaml
  patch:
    $ref: users/update-current-user.yaml
statistic:
  get:
    $ref: users/get-user-statistic.yaml
verify:
  get:
    $ref: users/verify-email.yaml
resend-verification:
  post:
    $ref: users/send-email-verification.yaml
password:
  put:
    $ref: users/change-password.yaml
password-auth0:
  put:
    $ref: users/change-auth0-password.yaml
